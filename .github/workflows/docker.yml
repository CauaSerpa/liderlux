name: Docker Build & Push

on:
  push:
    branches:
      - staging
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v6

        with:
          context: .
          file: docker/php/Dockerfile

          push: true

          tags: |
            ghcr.io/${{ github.repository_owner }}/liderlux:${{ github.ref_name }}
            ghcr.io/${{ github.repository_owner }}/liderlux:${{ github.sha }}